---
name: Reporte de bug
about: Algo del boilerplate no hace lo que dice su documentación
title: 'bug: '
labels: bug
assignees: ''
---

## Qué pasa

<!-- Una frase. El comportamiento observado, no la causa que sospechas. -->

## Qué debería pasar

<!-- Y dónde lo dice: enlaza el doc, la regla (`R{n}`) o el comentario del
     código que promete ese comportamiento. Un bug del boilerplate es una
     diferencia entre lo que hace y lo que documenta. -->

## Cómo reproducirlo

1.
2.
3.

## Entorno

- Versión del boilerplate (tag o commit):
- PHP (`php -v`):
- Sistema operativo:
- Base de datos:
- Toggles relevantes (`php artisan about --only=kore`):

```
```

## Salida del error

<!-- Traza completa, salida de `composer ci` o del comando que falla.
     Cuidado con pegar el .env: quita claves, tokens y contraseñas. -->

```
```

## Qué ya descartaste

<!-- Opcional pero muy útil: `php artisan config:clear`, `composer install`,
     un `.env` limpio desde `.env.example`, otra versión de PHP... -->
