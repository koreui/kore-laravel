<?php

declare(strict_types=1);

namespace App\Core\Actions;

abstract class Action
{
    /**
     * Each action exposes a single public entry point. Implement
     * `handle(...)` in subclasses with the parameters and return type
     * relevant to that use case.
     */
}
